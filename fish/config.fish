# Fish Shell Configuration
# ~/.config/fish/config.fish -> ~/dotfiles/fish/config.fish
#
# Organized into conf.d/ (auto-loaded alphabetically):
#   00-path.fish    - PATH setup (Homebrew, pyenv, nvm)
#   01-env.fish     - environment variables + secrets
#   02-abbr.fish    - abbreviations (aliases)
#
# Custom functions live in functions/ (auto-loaded by Fish)
